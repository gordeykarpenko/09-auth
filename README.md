# 03-react-movies

## Local setup

1. Copy `.env.example` to `.env`.
2. Set `VITE_TMDB_TOKEN` to your TMDB Bearer token.
3. Run `npm install` and `npm run dev`.

The production build is created with `npm run build` and can be deployed to Vercel.
